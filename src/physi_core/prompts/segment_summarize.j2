你正在为个人助手生成一段 30 分钟的活动记忆。
输入包含屏幕 OCR 片段、应用使用统计和剪贴板片段。
请调用 `write_segment` 工具一次，输出高信息量的摘要。

要求：
1. `summary`（Markdown 格式）：
   - 必须在开头包含一个 `**【Total】**` 一句话总结，概括用户当前最核心的活动或状态，方便后续快速查阅。
   - 说明用户在做什么，而不仅仅是开了哪个应用。
   - 包含具体的任务意图（例如：修复启动脚本、调试 QQ 桥、阅读 API 文档）。
   - 包含项目上下文（项目名 + 阶段：开发/调试/测试/发布）。
   - 包含知识上下文（用户正在学习/研究的概念、API 或错误）。
2. 通知决策：
   - 连续使用超过 2 小时 → should_notify=true（休息提醒）
   - 当前时间超过 01:00 且仍在活动 → should_notify=true（睡眠提醒）
   - 反复出现未解决的错误 → should_notify=true
   - 其他情况 → should_notify=false
3. 结构化提取：
   - `active_projects`：当前项目列表
   - `learning_topics`：当前学习/研究主题列表
   - `metadata_updates`：**只写真正稳定的生活/身份事实**，绝对禁止写入以下内容：
     * 任何"当前"状态（current_task、current_focus、当前工作文件、运行状态等）
     * 临时错误或异常（Everything未响应、VPN状态等）
     * 系统监控数据（token统计、trace数量、工具数量等）
     * 正在观看的视频、浏览的网页等一次性内容
     * 项目阶段、当前版本号等会频繁变化的数据
     合法的 metadata_updates 例子：新发现的账号/ID、服务器地址、作息时间、联系方式。
     如果不确定是否稳定，就不要写入，宁可不写。

[感知数据]
{{ data }}

[当前时间] {{ current_time }}
[连续使用时长（小时）] {{ "%.1f"|format(continuous_hours) }}
